@model IEnumerable<WardFormsCore.DataModel.RoleType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoleType1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RoleType1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RoleTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RoleTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RoleTypeId })
        </td>
    </tr>
}

</table>
