@model IEnumerable<WardFormsCore.Data.SkillType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SkillDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SkillDescriptionLocal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SkillDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SkillDescriptionLocal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SkillTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SkillTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SkillTypeID })
        </td>
    </tr>
}

</table>
