@model IEnumerable<WardForms.Models.FacilityGroupMember>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FKFGMFacilityGroupID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FacilityGroupMemberDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FromDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ThruDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FKFGMFacilityID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FKFGMFacilityGroupID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FacilityGroupMemberDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FromDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ThruDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FKFGMFacilityID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FGMID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FGMID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FGMID })
        </td>
    </tr>
}

</table>
