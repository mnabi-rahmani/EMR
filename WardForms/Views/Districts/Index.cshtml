@model IEnumerable<WardForms.Models.District>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Province.Province1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.District1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DistrictLocal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Province.Province1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.District1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DistrictLocal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DistrictCode }) |
            @Html.ActionLink("Details", "Details", new { id=item.DistrictCode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DistrictCode })
        </td>
    </tr>
}

</table>
